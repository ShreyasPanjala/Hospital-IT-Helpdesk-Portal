{
  "name": "hospital-it-helpdesk-portal",
  "version": "1.0.0",
  "private": true,
  "description": "Full-stack Hospital IT Helpdesk Portal matching the submitted abstract.",
  "scripts": {
    "install-all": "npm install && cd client && npm install && cd ../server && npm install",
    "client": "cd client && npm start",
    "server": "cd server && npm run dev",
    "dev": "concurrently \"npm run server\" \"npm run client\""
  },
  "devDependencies": {
    "concurrently": "^8.2.2",
    "netlify-cli": "^26.1.0"
  }
}
